# Marco AI Backend Requirements
# Memory-efficient dependencies for Raspberry Pi 3 (1GB RAM)

# Core framework
fastapi==0.109.0
uvicorn[standard]==0.27.0
python-multipart==0.0.6

# Database
# sqlite-vec is loaded as extension, not pip package

# LLM Clients
groq==0.4.2
openai==1.12.0
google-generativeai==0.3.2
google-api-python-client==2.118.0
google-auth-httplib2==0.2.0
google-auth-oauthlib==1.2.0

# Embeddings (optional, for RAG)
# sentence-transformers==2.3.1  # Uncomment if RAG semantic search needed

# Utilities
python-dotenv==1.0.0
pydantic==2.6.0
pydantic-settings==2.1.0

# HTTP client (lightweight)
httpx==0.26.0

# Session cookies (secure signed cookies for auth)
itsdangerous==2.1.2

# Note: Avoid heavy packages like langchain, llama-index
# Keep dependencies minimal for Raspberry Pi 3
